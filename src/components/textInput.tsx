import type { ReactNode } from "react";
import { NavLink } from "react-router";

type InputProps = {
    children: ReactNode;
}

function InputText() {
    return ( 
        
     );
}

export default InputText;